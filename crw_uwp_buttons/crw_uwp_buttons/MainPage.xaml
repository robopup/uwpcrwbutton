<Page
    x:Class="crw_uwp_buttons.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:crw_uwp_buttons"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Margin="12,10,12,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <RelativePanel>
                <Button x:Name="Button" Content="Write to File" HorizontalAlignment="Left" Margin="400,443,0,0" VerticalAlignment="Top" Height="83" Width="202" Click="writeFile_Click"/>
                <TextBlock x:Name="ButtonOutput" RelativePanel.RightOf="Button" TextWrapping="Wrap" Margin="50,475,120,475"/>
            </RelativePanel>
        </Grid>
    </Grid>
</Page>
